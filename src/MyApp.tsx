import React from 'react'

export default function MyAwesomeApp(): JSX.Element {
  return (
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: 20 }}>
      <h1>My Awesome App (Vite + React + TypeScript)</h1>
      <p>Este componente reemplaza al `App` original para parecerse al ejemplo del profesor.</p>
    </div>
  )
}
